[pytest]
markers =
    unit: unit tests
    nyc: tests that use the NYC road graph
    usa: tests that use the USA road graph
    performance: tests that measure runtime/speedup
    graceful_fallback: tests that assert C++ backend falls back to Python